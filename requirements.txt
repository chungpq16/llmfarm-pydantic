# Production Bosch LLM Farm Client
# Core dependencies only - clean and minimal

# OpenAI client for API communication
openai>=1.45.0

# HTTP client (used by OpenAI client)
httpx>=0.25.0

# Optional: For better performance
# uvloop>=0.19.0  # Uncomment for faster async on Linux/macOS